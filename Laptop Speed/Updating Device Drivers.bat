@echo off

:: This batch file updates device drivers to improve hardware performance.

:: Using the "devmgmt.msc" command to open the Device Manager window.
devmgmt.msc

:: Prompting the user to update device drivers manually.

:: Pausing the batch file to prevent it from closing automatically and to allow the user to update the drivers.
pause
