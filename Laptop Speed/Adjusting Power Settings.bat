@echo off

:: This batch file adjusts power settings to optimize for performance.

:: Using the "powercfg.cpl" command to open the Power Options window.
powercfg.cpl

:: Prompting the user to select a power plan that optimizes for performance.

:: Pausing the batch file to prevent it from closing automatically and to allow the user to select a power plan.
pause
