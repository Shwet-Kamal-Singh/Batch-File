@echo off
:: This batch file disables unnecessary startup programs to speed up the boot process.

:: Using the "msconfig" command to open the System Configuration window.
msconfig

:: Prompting the user to disable unnecessary startup programs manually.

:: Pausing the batch file to prevent it from closing automatically and to allow the user to disable the programs.
pause
